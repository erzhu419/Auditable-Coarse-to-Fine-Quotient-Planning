# K7 profile-native-zero semantic authority

Contract `2.0.27` / V0-110B-2E-17 closes exactly 114 V6 profile-native-zero
paths without interpreting a missing event as zero.  Issuance scans the frozen
production archive for every accounting-gateway call, requires the complete
89-site/71-owner-path inventory, and joins the exact occurrence window,
executed five-stage transcript, registered branch exclusions and legacy
replacement dependency DAG.  Every zero therefore has an obligation-specific
positive proof and recorder identity.

Unregistered source emission capability, incomplete owner windows, dependency
cycles, occurrence transplants, missing/noncanonical bytes and caller-minted
attestations fail closed.  This layer remains nonformal until it is joined
atomically to the nine shared, 71 owner and eight derived families.  It issues
no CounterRecord or vector and opens no Gate.
