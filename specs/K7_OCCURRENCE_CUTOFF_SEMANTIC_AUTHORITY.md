# K7 occurrence and cutoff semantic authority

Contract `2.0.25` / V0-110B-2E-15 upgrades the historical structural
occurrence join and cutoff attestation into replayable, nonformal semantic
authorities. It preserves the V1 objects and requires the exact production
role manifest, broker runtime, operational-output bytes, embedded child
business result, owned result/transcript, production-derived 71-path owner
candidate set, verified nine-resource envelope and durable-output evidence.

Issuance reconstructs the production owner candidates from the manifest's
actual transport archive and embedded business bytes, then requires exact
candidate-set equality. Request, route, scientific/logical occurrence,
runtime, manifest, source snapshot/archive, transcript terminal, measurement
window and output identities must all join. The registered fixture freezes:

```text
terminal_status = CHILD_ACTION_ROW_CAP_EXCEEDED
terminal_kind = COMPLETED
route_attempt_outcome = FAILURE
(route.attempts, route.successes, route.failures) = (1, 0, 1)
```

The global marker order is a closure index, not a renumbering of the nine
source-local journals. Each source retains its own closed interval. The four
post-cutoff accounting/provenance components are replayed from independently
held bytes and proved absent from the eight durable output-role charges.

The six authority domains are centrally registered and role-separated. This
contract is only a prerequisite for the atomic 202-path materializer: it does
not issue a `CounterRecord`, `WorkVector`, `ComparisonVector`, projection,
terminal certificate or official execution authority. Counter Completeness,
workload economics, scalar/break-even, fresh science and certificate Gates
remain locked or `NOT_RUN`.
