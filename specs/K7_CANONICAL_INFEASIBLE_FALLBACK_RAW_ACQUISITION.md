# K7 canonical infeasible fallback raw acquisition

Contract `2.0.42` / V0-110B-2E-31 binds the canonical G2048 H=1 exact
infeasibility witness to an independently supplied current source/kernel/query
identity, freezes a marginal `DIRECT_FALLBACK` decision before ground access,
and executes the existing raw in-process fallback solver once.

This is a source-value acquisition, not production-native V6 accounting.  The
raw solver has `trusted_provenance = null`, emits the historical 42-row V1
WorkVector, and does not cover the complete FQ9 occurrence stage plan.  Its
seven retained operation values and six route/solver reconciliation values are
kept as typed legacy candidates; none is relabelled as a V6 CounterRecord.

The exact V6-path partition is:

```text
178  DIRECT_FALLBACK stage-forbidden zero candidates without native-zero proof
  7  source-bound legacy native-value candidates
  6  source-bound legacy reconciliation-value candidates
  9  unresolved shared-resource receipts
  2  unresolved process-exit derived proofs
---
202  formally blocked paths
```

Thus the acquisition retains 13 exact source values but resolves zero formal
paths.  In particular, the 178 stage-forbidden values are not native zeros:
the stage profile says where nonzero work is allowed, but no complete
production recorder or side-effect monitor has proved the absence of every
forbidden operation.

The current identity is issuer-owned and must be constructed independently
from the current source bundle, live kernel/query law, and explicit
BuildEpoch, threshold, reward, policy-class and complete-search identities.
The claimant proof cannot supply its own current identity by default.  Stale,
bare, substituted-callable and post-freeze side-effect attacks fail closed.
`CAP_EXHAUSTED` remains a noncertificate fallback acquisition and is never
reclassified as infeasibility.

Seven content domains are centrally registered and role-separated.  No
production WorkVector, ComparisonVector, terminal, occurrence closure,
certificate, scalar, break-even or Gate authority is issued.  The complete
PREOPEN/acquisition/build/failed-abstract/fallback/closed chain, native V6
recorders, nine shared receipts, formal derived proofs and independent
all-path verifier remain successor obligations.
