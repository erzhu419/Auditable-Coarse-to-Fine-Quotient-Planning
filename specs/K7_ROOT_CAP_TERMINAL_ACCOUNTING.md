# K7 root-cap terminal accounting

Contract `2.0.30` / V0-110B-2E-20 derives the production attempt terminal from
the genuine child-closure result and complete formal work.  Trusted replay
proves that existing plus unresolved child action rows exceed the registered
cap of 19 and binds the exact business bytes, full evidence roots, route
identity and 202 records.  The only result is:

```text
ROUTE_ATTEMPT
ATTEMPT_CLOSURE_NONCERTIFICATE
ATTEMPT_BUDGET_EXHAUSTED
```

The worker's status is not authoritative, cap exhaustion is never
infeasibility, and an ID-only materialization cannot hide work.  Forged cap,
terminal class/code, incomplete vector, occurrence transplant and Gate unlock
all fail closed.  This closes one route attempt only; logical-occurrence and
campaign closure remain separate authorities.
